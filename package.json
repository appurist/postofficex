{
  "name": "postofficex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.js",
    "test": "bun test",
    "build": "bun build --compile ./src/index.js --outfile postofficex"
  }
}
